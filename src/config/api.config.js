// ============================================
// CONFIGURACIÓN DE ENDPOINTS DEL BACKEND
// ============================================
// Aquí solo necesitas copiar y pegar los endpoints que te dé el backend

export const API_CONFIG = {
    // Endpoint para obtener los datos de los selects del formulario (catálogo)
    FORM_DATA_ENDPOINT: '/api/v1/catalog',

    // Endpoint para enviar el formulario de registro
    SUBMIT_FORM_ENDPOINT: '/api/v1/users/register',
};

// ============================================
// NO MODIFICAR NADA ABAJO DE ESTA LÍNEA
// ============================================

export default API_CONFIG;
